# Site Scoring ML Module - Dependencies
# Optimized for Apple M4 with MPS backend

# Core ML
torch>=2.1.0          # MPS support for Apple Silicon
numpy>=1.24.0

# Fast data loading
polars>=0.19.0        # 10-20x faster than pandas for large CSV

# Preprocessing
scikit-learn>=1.3.0   # LabelEncoder, StandardScaler
